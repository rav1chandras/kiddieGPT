# ── Secrets — NEVER commit ─────────────────────────────────────────────
# Extension: local-settings.js holds a real OpenAI key + local portal URL.
# Copy extension/local-settings.example.js -> extension/local-settings.js.
local-settings.js
**/local-settings.js

# Environment files (portal)
.env
.env.*
!.env.example
!.env.redacted

# Portal live runtime DB — contains API keys + parent/child PII.
data/kiddiegpt-db.json
**/data/kiddiegpt-db.json
**/data/*.local.json

# ── Dependencies ───────────────────────────────────────────────────────
node_modules/
**/node_modules/

# ── Build artifacts / archives ─────────────────────────────────────────
*.zip

# ── OS / editor / tooling ──────────────────────────────────────────────
.DS_Store
.claude/
*.log
